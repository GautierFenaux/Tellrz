{% extends 'base-admin.html.twig' %}

{% block title %}Reporting{% endblock %}

{% block body %}
    <h1>Reporting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reporting.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ reporting.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_reporting_index') }}">back to list</a>

    <a href="{{ path('admin_reporting_edit', {'chapterId': reporting.chapter.id, 'manuscriptId': reporting.manuscript.id, 'id': reporting.id}) }}">edit</a>

    {{ include('admin_reporting/_delete_form.html.twig') }}
{% endblock %}
