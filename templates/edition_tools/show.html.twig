{% extends 'base.html.twig' %}

{% block title %}{{ manuscript.title }}{% endblock %}

{% block body %}
    

     <div class="d-flex flex-row justify-content-between">
        <h1>Votre Manuscrit</h1>
        <a href="{{ path('edition_tools_index') }}" class="btn btn-dark"><strong>Retour à la liste</strong></a>
    </div>

    <table class="table table mt-3">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ manuscript.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ manuscript.content }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ manuscript.description }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('edition_tools_edit', {'id': manuscript.id}) }}">edit</a>

    {{ include('edition_tools/_delete_form.html.twig') }}
{% endblock %}
