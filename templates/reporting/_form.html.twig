{{ form_start(form, {'action' : path('reporting_new', {'chapterId': chapterId, 'manuscriptId': manuscriptId} )}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Envoyer') }}</button>
{{ form_end(form) }}
